<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.바른글씨" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_main</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color.-->

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="navigationBasic">@color/color_navigationBasic</item>
        <item name="navigationSelect">@color/color_navigationSelect</item>
        <item name="yes">@color/color_yes</item>
        <item name="no">@color/color_no</item>
        <item name="intoBookmark">@color/color_intoBookmark</item>
        <item name="arrow2">@color/color_arrow2</item>
    </style>
</resources>